<%= form_for(task) do |f| %>
<% if task.errors.any? %>

  <div id="error_explanation">
      <ul>
          <% task.errors.full_messages.each do |msg| %>
          
          
             <li>
                <%= task %>
                
             </li>
             <% end %>
      </ul>
  </div>
  
  <% end %>
 <%= f.label :status,"title" %>
 <%= f.text_field:status %>
 
 
 <%= f.label :content,"タスク"%>
 
 <%= f.text_field :content %>
 
 
 <%= f.submit "追加" %>
 <% end %>
 